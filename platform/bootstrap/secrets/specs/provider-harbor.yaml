apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: provider-harbor-credentials
spec:
  namespace: crossplane-system
  type: Opaque
  vault:
    path: applications/security/crossplane/providers/harbor
  data:
    credentials:
      env: PROVIDER_HARBOR_CREDENTIALS
      cache: false
