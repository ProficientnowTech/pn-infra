apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: keycloak-admin-secret
spec:
  namespace: keycloak
  type: Opaque
  vault:
    path: applications/security/keycloak/admin
  data:
    admin-password:
      generate:
        length: 48
