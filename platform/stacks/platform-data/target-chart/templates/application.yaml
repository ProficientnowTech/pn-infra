{{- range .Values.applications }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.argocdNamespace | default "argocd" }}
  annotations:
    {{- $annotations := dict }}
    {{- if $.Values.commonAnnotations }}
    {{- $annotations = merge $annotations $.Values.commonAnnotations }}
    {{- end }}
    {{- if .annotations }}
    {{- $annotations = merge $annotations .annotations }}
    {{- end }}
    {{- if $annotations }}
    {{- toYaml $annotations | nindent 4 }}
    {{- end }}
  labels:
    app.kubernetes.io/name: {{ .name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
    app.kubernetes.io/part-of: {{ $.Values.stackName | default "platform" }}
    {{- if $.Values.commonLabels }}
    {{- toYaml $.Values.commonLabels | nindent 4 }}
    {{- end }}
    {{- if .labels }}
    {{- toYaml .labels | nindent 4 }}
    {{- end }}
spec:
  project: {{ .project | default $.Values.global.project | default "default" }}
  {{- if .sources }}
  sources:
    {{- range .sources }}
    - repoURL: {{ .repoURL }}
      targetRevision: {{ .targetRevision | default "main" }}
      {{- if .chart }}
      chart: {{ .chart }}
      {{- end }}
      {{- if .path }}
      path: {{ .path }}
      {{- else if .ref }}
      ref: {{ .ref }}
      {{- end }}
      {{- if .helm }}
      helm:
        {{- if .helm.releaseName }}
        releaseName: {{ .helm.releaseName }}
        {{- end }}
        {{- if .helm.valueFiles }}
        valueFiles:
          {{- range .helm.valueFiles }}
          - {{ . }}
          {{- end }}
        {{- end }}
        {{- if .helm.parameters }}
        parameters:
          {{- range .helm.parameters }}
          - name: {{ .name }}
            value: {{ .value | quote }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- end }}
  {{- else }}
  source:
    repoURL: {{ .repoURL | default $.Values.default.repoURL }}
    targetRevision: {{ .targetRevision | default $.Values.default.targetRevision | default "main" }}
    {{- if .chart }}
    chart: {{ .chart }}
    {{- else }}
    path: {{ .path | default (printf "platform/stacks/platform-data/charts/%s" .name) }}
    {{- end }}
    {{- if .helm }}
    helm:
      {{- if .helm.releaseName }}
      releaseName: {{ .helm.releaseName }}
      {{- end }}
      {{- if .helm.valueFiles }}
      valueFiles:
        {{- range .helm.valueFiles }}
        - {{ . }}
        {{- end }}
      {{- end }}
      {{- if .helm.parameters }}
      parameters:
        {{- range .helm.parameters }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
      {{- end }}
    {{- end }}
  {{- end }}
  destination:
    server: {{ .destinationServer | default "https://kubernetes.default.svc" }}
    namespace: {{ .destinationNamespaceOverwrite | default (.namespace | default .name) }}
  syncPolicy:
    {{- if .syncPolicy }}
    {{- toYaml .syncPolicy | nindent 4 }}
    {{- else }}
    automated:
      prune: {{ $.Values.global.syncPolicy.automated.prune | default true }}
      selfHeal: {{ $.Values.global.syncPolicy.automated.selfHeal | default true }}
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
      {{- if .syncOptions }}
      {{- range .syncOptions }}
      - {{ . }}
      {{- end }}
      {{- end }}
    {{- if $.Values.global.retry }}
    retry:
      {{- toYaml $.Values.global.retry | nindent 6 }}
    {{- end }}
    {{- end }}
  {{- if .ignoreDifferences }}
  ignoreDifferences:
    {{- toYaml .ignoreDifferences | nindent 4 }}
  {{- end }}
{{- end }}
