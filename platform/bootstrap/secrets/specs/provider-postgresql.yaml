apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: provider-postgresql-credentials
spec:
  namespace: crossplane-system
  type: Opaque
  vault:
    path: applications/security/crossplane/providers/postgresql
  data:
    credentials:
      env: PROVIDER_POSTGRESQL_CREDENTIALS
      cache: false
