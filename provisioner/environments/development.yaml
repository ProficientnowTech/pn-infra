# Provisioner Environment: Development
# Not used for Kind deployment (Kind uses pre-built container images)

configPackage: core
environment: development

# SSH configuration (not applicable for Kind)
ssh:
  private_key_path: ~/.ssh/id_rsa
  user: ubuntu
  port: 22
