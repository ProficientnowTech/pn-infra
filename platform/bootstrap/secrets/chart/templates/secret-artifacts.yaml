{{- $manifests := .Files.Glob "files/manifests/**/*.yaml" }}
{{- if $manifests }}
{{- range $path, $_ := $manifests }}
---
# Source: {{ $path }}
{{ tpl ($.Files.Get $path) $ | trimSuffix "\n" }}
{{- end }}
{{- end }}
