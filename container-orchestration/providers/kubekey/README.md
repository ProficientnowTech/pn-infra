# Kubekey Provider (placeholder)

Add scripts and docs here to deploy Kubernetes clusters via Kubekey using API-generated inventories. Implement `deploy.sh`, `reset.sh`, and `validate.sh` compatible with `container-orchestration/README.md`, pulling inputs from `api/outputs/<env>/`.
