# Azure Provider (Placeholder)

Implement Terraform modules and scripts here to provision infrastructure on Azure. Ensure the provider reads config packages + API outputs and exposes `plan/apply/destroy` for `deploy.sh`.
