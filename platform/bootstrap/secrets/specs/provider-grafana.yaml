apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: provider-grafana-credentials
spec:
  namespace: crossplane-system
  type: Opaque
  vault:
    path: applications/security/crossplane/providers/grafana
  data:
    credentials:
      env: PROVIDER_GRAFANA_CREDENTIALS
      cache: false
