# Container Orchestration Environment: Development
# Using Kind for local Kubernetes development

configPackage: core
environment: development
provider: docker

# Kind-specific configuration overrides
# (merged with config/packages/core/orchestrators/kind.yaml)
