apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: pcp-harbor-client-secret
spec:
  namespace: keycloak
  type: Opaque
  vault:
    path: applications/security/keycloak/clients/harbor
  data:
    client-id:
      literal: harbor
      cache: true
    client-secret:
      generate:
        length: 48
