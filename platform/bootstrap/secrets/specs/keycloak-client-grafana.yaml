apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: pcp-grafana-client-secret
spec:
  namespace: keycloak
  type: Opaque
  vault:
    path: applications/security/keycloak/clients/grafana
  data:
    client-id:
      literal: grafana
      cache: true
    client-secret:
      generate:
        length: 48
