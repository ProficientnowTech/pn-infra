apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: provider-kafka-credentials
spec:
  namespace: crossplane-system
  type: Opaque
  vault:
    path: applications/security/crossplane/providers/kafka
  data:
    credentials:
      env: PROVIDER_KAFKA_CREDENTIALS
      cache: false
