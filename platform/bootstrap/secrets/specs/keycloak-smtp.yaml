apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: keycloak-smtp-secret
spec:
  namespace: keycloak
  type: Opaque
  vault:
    path: applications/security/keycloak/smtp
  data:
    smtp-password:
      env: KEYCLOAK_SMTP_PASSWORD
      cache: false
