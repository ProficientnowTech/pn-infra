apiVersion: platform.pnats.cloud/v1alpha1
kind: SecretSpec
metadata:
  name: provider-temporal-credentials
spec:
  namespace: crossplane-system
  type: Opaque
  vault:
    path: applications/security/crossplane/providers/temporal
  data:
    credentials:
      env: PROVIDER_TEMPORAL_CREDENTIALS
      cache: false
