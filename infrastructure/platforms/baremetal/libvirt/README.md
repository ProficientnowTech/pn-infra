# Libvirt Provider (Placeholder)

Add Terraform/Ansible assets here to provision bare metal or virtualized hosts via Libvirt. Follow the same interface as `platforms/proxmox/terraform` (shared `run.sh` flags, tfvars generated from config packages).
