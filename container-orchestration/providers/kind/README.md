# Kind Provider (placeholder)

Add scripts and docs here to deploy Kubernetes clusters via Kind for local testing using API-generated inventories. Reuse the provider contract from Kubespray (`deploy.sh`, `reset.sh`, `validate.sh`) and respect `container-orchestration/environments/<env>.yaml` + `config` packages.
