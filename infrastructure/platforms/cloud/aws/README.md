# AWS Provider (Placeholder)

Implement Terraform modules and scripts here to provision infrastructure on AWS. Consume config packages (`infrastructure/environments/<env>.yaml`) and API outputs just like the Proxmox Terraform stack.
